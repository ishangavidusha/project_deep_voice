import 'package:flutter/material.dart';

class DeepMainLogo extends StatelessWidget {
  const DeepMainLogo({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text(
      'Deep Voice Lanka',
      style: Theme.of(context).textTheme.headline5
    );
  }
}